services:
  api:
    build: .
    env_file: .env
    ports:
      - "8080:8080"
  web:
    image: nginx:alpine
    volumes:
      - ./apps/web/dist:/usr/share/nginx/html:ro
      - ./infra/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "3000:80"
